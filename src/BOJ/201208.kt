package BOJ

import java.io.BufferedReader
import java.io.InputStreamReader

/*
 1. 체스판 다시 칠하기 (X)
 2. 영화감독 숌
*/

fun main() = with(BufferedReader(InputStreamReader(System.`in`))) {
    // 2
    /*
    var input = readLine().toInt()
    var number = 0

    while (input > 0) {
        number++
        if(number.toString().contains("666")) input--
    }

    print(number)
    */
}